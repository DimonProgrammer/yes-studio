{
  "version": 2,
  "buildCommand": "node build.js",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/blog/(.*)", "dest": "/blog/$1" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
