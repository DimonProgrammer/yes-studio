# Синхронизация с Notion

Этот проект настроен на автоматическую синхронизацию папки "Yes Studio" из Notion.

## 📋 Требования

1. **Notion Integration** подключена к папке "Yes Studio"
   - [Создать интеграцию](https://www.notion.com/my-integrations)
   - Дать доступ к папке "Yes Studio" (Connections → Add connection)

2. **API Key** сохранён в переменной окружения `NOTION_API_KEY`

## 🚀 Использование

### Локально (вручную)

```bash
npm run sync:notion
```

Или через bash:
```bash
bash scripts/sync-notion-local.sh
```

Файлы будут сохранены в `docs/from-notion/`

### GitHub Actions (автоматически)

1. Добавь `NOTION_API_KEY` в GitHub Secrets:
   - Перейди в Settings → Secrets and variables → Actions
   - Нажми "New repository secret"
   - Name: `NOTION_API_KEY`
   - Value: твой API ключ из Notion

2. Синхронизация будет запускаться:
   - **Каждые 6 часов** (по расписанию)
   - **Вручную** через Actions → "Sync Notion with Project"

## 📁 Структура

После синхронизации создаётся:
```
docs/from-notion/
├── страница-1.json
├── страница-2.json
└── ...
```

Каждый файл содержит:
```json
{
  "title": "Название страницы",
  "created": "2026-02-18T...",
  "updated": "2026-02-18T...",
  "blocks": [
    {
      "type": "paragraph",
      "text": "Текст параграфа"
    },
    ...
  ]
}
```

## 🔧 Конфигурация

Отредактируй `scripts/sync-notion.js` если нужно:
- Изменить расположение папки для сохранения
- Добавить фильтр по типам страниц
- Расширить извлечение контента (сейчас извлекаются основные блоки)

## ⚠️ Безопасность

- **Никогда** не коммить `.env` файл с API ключом!
- Используй GitHub Secrets для CI/CD
- Регенерируй API ключ если он был скомпрометирован

## 💡 Примеры

### Использование синхронизированного контента в React

```javascript
import faqContent from '../docs/from-notion/faq.json';

function FAQ() {
  return (
    <div>
      <h1>{faqContent.title}</h1>
      {faqContent.blocks.map(block => (
        <p key={block.id}>{block.text}</p>
      ))}
    </div>
  );
}
```

### Автоматическая обработка после синхронизации

Если нужна обработка файлов после синхронизации, можно добавить post-sync скрипт.

---

📞 **Вопросы?** Отредактируй этот файл или обнови скрипт синхронизации.
